{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run func-extensions-install & tsc -b & ncp shared dist/shared",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "func-extensions-install": "func extensions install"
  },
  "dependencies": {
    "durable-functions": "^1.4.3"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "typescript": "^3.6.4",
    "ncp": "^2.0.0"
  }
}
